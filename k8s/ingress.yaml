apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: review-agent-ingress
  namespace: ai-reviewer
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - http:
        paths:
          - path: /webhook
            pathType: Prefix
            backend:
              service:
                name: review-agent
                port:
                  number: 80
